{
  "name": "@lorenstuff/fritter-api-utilities",
  "version": "4.0.1",
  "description": "A collection of utilities for building APIs with the Fritter web framework.",
  "main": "./build/index.js",
  "scripts": {
    "build": "npx rimraf build && npx tsc",
    "build:watch": "npx rimraf build && npx tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "fritter",
    "api",
    "utilities"
  ],
  "author": "Loren Goodwin",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@lorenstuff/typescript-config": "^2.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  },
  "peerDependencies": {
    "@lorenstuff/fritter": "^5.0.0",
    "zod": "^3.24.4"
  }
}
